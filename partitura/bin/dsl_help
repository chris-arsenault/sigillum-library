#!/usr/bin/env ruby
# frozen_string_literal: true

require "optparse"
require_relative "../lib/partitura/orchestral_dsl"

json = false
parser = OptionParser.new do |opts|
  opts.banner = "Usage: dsl_help TOPIC [--json]"
  opts.on("--json", "Emit JSON response") { json = true }
end

parser.parse!
topic = (ARGV.shift || "index").to_sym

if json
  puts Sigillum::OrchestralDSL::JITDocs.render_json(topic)
else
  puts Sigillum::OrchestralDSL::JITDocs.render(topic)
end
